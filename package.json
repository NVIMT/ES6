{
  "name": "app",
  "version": "0.1.0",
  "scripts": {
    "compile": "babel -d lib src",
    "prepublish": "npm run compile"
  },
  "main": "lib/app.js",
  "dependencies": {
    "core-js": "^0.6.0"
  },
  "devDependencies": {
    "babel": "^4.6.0"
  }
}